# 本地读书卡库

公开 ResearchOS 仓库不保存真实读书卡。这里仅保留目录说明，真实 `cards/` 和 `indexes/` 应由用户在本地生成，并由 `.gitignore` 排除。

推荐边界：

- 公开仓库：只保存模板和使用说明。
- 本地实例：可保存集中读书卡、主题索引和项目索引。
- 具体课题目录：保存项目自己的读书卡、用途说明、阅读计划和综述矩阵。

如果需要传播示例，请使用脱敏样例或模板，不要提交真实 Zotero 条目 key、未公开课题说明、个人路径或全文摘录。
