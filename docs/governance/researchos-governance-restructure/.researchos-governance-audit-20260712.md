# `.researchos` 治理审计（2026-07-12）

## 范围与边界

本次只治理 ResearchOS 工作区内的 `.researchos/`。未读取或修改
`.sandbox-secrets`，未连接或修改 Zotero，也未处理具体课题成果。

## 执行结果

- 盘点到 130 个文件，其中 121 个位于 `outputs/`，总量约 8.34 MB。
- 删除 `outputs/machine/` 根部 21 个无仓库引用、可再生成的 `temp-*` 文件。
- 将 6 个 Zotero 写入运行包从
  `outputs/machine/M-002-library-governance/zotero-unfiled-write-runs/`
  整体迁移到
  `outputs/archive/A-001-library-governance/zotero-unfiled-write-runs/`。
- 归档包含 3 次 preflight 和 3 次 write；三次实际写入分别记录 1、1、63
  个条目，`blocked_rows` 均为 0。
- 在归档目录生成 `manifest.json` 和 `SHA256SUMS.txt`，用于清点和完整性验证。
- 更新 `.researchos/README.md`，明确指针、配置、批注、机器留存和外部写入
  审计五类资产边界。

## 分类结论

| 类别 | 位置 | 生命周期 |
|---|---|---|
| 跨端指针 | `.researchos/` 根部 | 长期保留 |
| 非秘密配置 | `.researchos/providers/` | 长期保留，禁止凭据 |
| 人工批注状态 | `.researchos/human-annotation-inbox/` | 按处理流程归档 |
| 可再生机器产物 | `.researchos/outputs/machine/M-###-*/` | 任务期保留，无引用可清理 |
| 外部写入证据 | `.researchos/outputs/archive/A-###-*/` | 长期保留 |

## 高风险工具闭环

后续代码审计确认，`execute_zotero_additive_write_plan.py` 与
`execute_zotero_deleted_collection_cleanup.py` 都曾把运行包写入通用机器目录。
两个脚本现已改用 `A001_LIBRARY_GOVERNANCE` 归档常量；输入 dry-run 计划仍从
`M002_LIBRARY_GOVERNANCE` 读取。项目 collection overlay 工具继续要求调用方
显式提供 `--runs-dir`，没有新增隐式默认路径。

本次同时补充了默认路径回归测试。历史资产迁移和未来运行落点现已闭环。

随后对写入工具进行了职责与重复性审计。三个执行脚本的认证、代理、HTTP 请求和
审计辅助逻辑存在局部重复，但分别承担项目文献集覆盖层、通用追加式写入和已删除
collection 引用修复，输入契约与回滚语义不同，因此未合并。四个 Python 文件整体
迁移到语义更明确的 `tools/zotero/write/`；策略、契约、评测、测试和导入路径已同步，
迁移不改变其高风险属性和逐次审批要求。
